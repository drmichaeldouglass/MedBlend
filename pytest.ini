[pytest]
testpaths = tests
# tests/ has no __init__.py, so pytest prepends it to sys.path and
# tests/conftest.py loads the add-on with the bpy stubs before collection.
filterwarnings =
    ignore::UserWarning:pydicom.valuerep
